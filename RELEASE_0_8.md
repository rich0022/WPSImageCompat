# 0.8.0 — 受控工作簿转换

## 已交付功能

- Convert Workbook 在用户勾选确认后修改当前工作簿；操作前应由用户保存副本。
- 已解析的 DISPIMG 图片创建为永久 Excel Shape，并在图片成功创建后才清除对应公式；现有插件预览会被永久图片替换，Remove Preview Images 不会删除永久图片。
- 可分别选择将当前非错误的 `_xlfn` / `_xlws` 公式和外部引用公式冻结为常量值。不会以字符串方式猜测函数等价式，也不会打开外部工作簿。
- `#REF!` 始终保留供人工修复，避免凭空指定原始引用。
- 检查报告详情截断时，冻结公式操作拒绝部分执行，避免只转换未知的一部分。
- Cloudflare 构建会拒绝部署 `PHASE*.md` 与 `RELEASE_*.md` 等内部记录；生产站点只提供用户功能与帮助页面。

## 验证

- TypeScript、自动测试、生产构建和生产 manifest 验证通过。
- 自动测试覆盖永久 Shape 去重、仅替换插件预览、转换计划去重、未选项不转换、#REF! 不猜测、详情截断拒绝部分冻结。

## 仍需真实宿主验收

Windows/macOS Excel 中必须用副本确认：Shape 保存后仍存在、保护表/隐藏单元格的失败提示、冻结值的业务含义、实际 WPS 文件快照是否保留图片部件。Office.js 无法保证未经用户选择的单独输出文件，因此本版不自动另存或覆盖备份。
