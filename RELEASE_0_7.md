# 0.7.0 — 汇总定位与线上团队版

## 本版变化

- 兼容检查页面按“问题类型 + 判断状态”汇总，每类只显示一行及完整数量，不再逐条展开到窗格。
- 有实时单元格来源的问题显示“定位到 工作表!单元格”。点击后激活该工作表并选中首个匹配单元格；不写入值、公式、格式、图片或工作簿内容。
- 详细兼容报告仍保存每一条已保留的发现，供需要时本地下载；窗格的汇总数量包含超出详情上限的同类问题。
- 升级 manifest 到 `1.4.0.0`，项目版本升级至 `0.7.0`。
- 明确区分 localhost 开发清单和 Cloudflare 生产清单，并补充多人分发及线上更新说明。

## 验证

- TypeScript 类型检查通过。
- 自动测试 85 项通过，覆盖汇总计数、详情截断后计数、导航选择、无效地址及既有图片/兼容流程。
- 构建生成生产 manifest，生产 URL 不含 localhost。

## 限制

- “定位到”只适用于来自当前 Excel 单元格扫描的发现；工作簿 XML 的外部引用记录没有单元格位置，无法跳转。
- 如果工作表被删除、重命名或宿主拒绝激活隐藏/受保护位置，会提示无法定位，且不改变其他单元格。
- 团队成员首次仍需安装生产 manifest；没有 Microsoft 365 管理员集中部署或 AppSource 发布时，Cloudflare 不能替代 Excel 的安装流程。
