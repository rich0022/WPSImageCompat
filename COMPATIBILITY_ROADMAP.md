# WPS / Excel 兼容助手：扩展评估

本文件区分已实现和后续方案。0.6 已实现只读公式、外部引用盘点与日期系统报告，详见 RELEASE_0_6.md；不包含外部目标验证或自动修复。0.5 已交付：DISPIMG 图片资源解析与预览、异常处理、诊断、中英任务窗格及发布更新提示。完整的 Windows/macOS 原生图片验收仍需继续；真实磁盘样本解析通过不能替代宿主验证。

## 建议定位

先做 **在 Microsoft Excel 中运行的 WPS 文件兼容检查与修复助手**。Windows 和 macOS 使用同一套 Office Add-in 架构，优先解决 WPS 创建的 XLSX 在 Excel 中的可验证问题。

不承诺识别所有差异或一键修复所有文件。当前产品、仓库名称和 manifest ID 保持不变；待第二类兼容功能有稳定用户价值后再决定是否更名。

“帮助 Excel 读取 WPS 文件”与“插件本身运行在 WPS 内”是两项不同工作。当前项目只实现 Excel 宿主适配；若以后增加 WPS 内插件，应单独验证 WPS 的加载项平台、接口和 Windows/macOS 支持情况，不能仅靠更换清单承诺兼容。纯规则和 OOXML parser 可以复用，宿主操作需要独立适配。

## 可做的方向与边界

| 类别 | 可以交付的结果 | 首期修复策略 | 证据要求 |
| --- | --- | --- | --- |
| DISPIMG 图片 | 定位公式、映射内嵌资源、显示和清理预览 | 沿用现有预览；正式转换只输出副本 | 磁盘原文件与 Excel 快照分别核对，验证显示/保存 |
| 不受支持的公式 | 标记函数、所在位置、当前错误值、宿主版本及候选处理建议 | 先只报告；有等价性测试的单条规则才允许转换副本 | `_xlfn`、实际求值结果及版本上下文，不能仅凭名字认定 WPS 缺陷 |
| 外部链接与失效引用 | 列出外部依赖、引用位置及检查状态 | 不自动访问外部路径，不自动断开链接或替换值 | 从公式/OOXML/受支持 API 获取引用，无法验证目标时明确“未检查” |
| 日期、数值与文本 | 报告日期系统、潜在文本数字和精度风险 | 用户逐项确认含义后处理副本 | 与原值、格式、来源及使用场景对照；不能凭数字猜日期 |
| 格式、合并与打印布局 | 记录潜在布局差异和人工核对位置 | 先给检查清单，不承诺像素一致 | 需要 WPS 原始渲染作为基准；单个 Excel 快照不足以证明差异 |
| 图表、透视表、连接、宏或嵌入对象 | 将来按文件类型和 API 能力盘点对象 | 首期报告“需要原应用处理”，不执行宏或自动重建复杂对象 | 每种对象单独研究并收集真实样本，不能把存在对象等同不兼容 |

原文件已经丢失的图片字节、已截断的编号或已丢失的数值精度，没有来源数据时无法可靠还原。应报告不可恢复或需要原文件，不生成猜测内容。

## 检测和修复应分开

一次兼容检查输出：问题类别、位置、严重程度、实际证据、是否已确认、当前宿主能力，以及可执行操作。

建议使用四种判断状态：**已确认问题、潜在风险、未检查、已检查未发现问题**。例如，发现外部引用只能说明存在依赖；没有访问目标文件，不应直接标记“链接失效”。发现 `@` 也不是错误。

扫描始终只读。图片预览继续保留原始公式。更改公式、值或格式属于显式 Convert/修复操作，先展示将变更的单元格和规则，再输出新工作簿；原文件不覆盖。若某规则无法保证结果等价，只给建议。

现有隐私诊断继续只导出计数和代码。未来“详细兼容报告”会包含单元格/公式等敏感业务内容，应与简要诊断分开、默认仅本地下载，不自动上传。

## 建议开发顺序

1. **稳固图片流程**：完成 Windows/macOS 的 Show、重复 Show、Refresh、Remove、保存再打开验收。若 Excel 快照确实丢失 WPS 部件，再增加用户主动选择原始 XLSX 的本地读取回退，并校验它与当前工作簿是否匹配；不要默默把另一个文件的图片插入当前表。
2. **0.6：只读兼容检查（已实现限定范围）**：包含公式错误与不受支持函数、外部引用、日期系统这三组规则。提供问题列表、检查范围、未检查原因和本地报告。保持 DISPIMG 模块独立。
3. **后续：受控转换**：先实现最有需求的一种图片转换到新工作簿，再按真实样本增加可证明等价的公式规则；每条规则都有独立测试和结果核对。
4. **再扩对象与宿主**：根据用户样本决定图表/布局等优先级；WPS 内运行及 Excel → WPS 的反向检查单独立项，不混入首轮 API 假设。

0.6 的验收标准：每条报告可追溯到文件或宿主证据；普通 Excel 文件不被误判为 WPS 故障；重复扫描一致；不写入任何原始值/公式；取消和 API 能力不足可正常退出。缺少代表性样本的规则不标记为正式支持。

## 保持轻量的实现方式

保留 TypeScript、Vite、Office.js、JSZip 与独立 parser。不为扩展提前加入 React、服务端文件上传、Python 或平台专有组件。

后续将现有读取能力汇总为只读扫描上下文，每一组规则使用独立模块，返回统一的问题结构。渲染 UI 只负责显示问题；执行预览和转换的模块单独存在，不允许“扫描规则”偷偷修改工作簿。每条规则声明所需 Excel API 或 OOXML 部件，不支持时返回“未检查”。

先在新增兼容检查功能时引入这些接口，不为路线图提前重构当前稳定的图片处理代码。

## 技术依据

- [微软：_xlfn 前缀](https://support.microsoft.com/en-us/excel/issue-an-xlfn-prefix-is-displayed-in-front-of-a-formula)：它与当前 Excel 版本不支持函数有关，不能全部归因于 WPS。
- [微软：隐式交叉运算符 @](https://support.microsoft.com/en-us/excel/implicit-intersection-operator)：该符号具有计算语义，不能全局删除。
- [微软：Excel 日期系统](https://support.microsoft.com/en-US/Excel/date-systems-in-excel)：1900/1904 系统的不同会影响日期解释，但需要上下文确认后处理。
- [微软：宿主与 API 要求](https://learn.microsoft.com/en-us/office/dev/add-ins/develop/specify-office-hosts-and-api-requirements)：每项功能需检查宿主能力，不能由“插件可安装”推导所有 API 都可用。

这是一份依据现有项目和官方 API 边界做出的产品与工程建议，尚无足够真实样本证明各类 WPS 差异的发生频率；优先级应继续由用户遇到的问题验证。
